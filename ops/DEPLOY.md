# Deployment: Giga-Native Validator

## Prerequisites
- System requirements for 12+ core throughput.
- `LimitMEMLOCK` must be set to `209715200` to support our zero-copy memory views.

## The Giga-Native Difference
Unlike standard `seid` deployments, running a Giga-Native node requires manual linkage of the optimized execution kernel. 

## Standard Procedure
(Paste your node instructions here)
